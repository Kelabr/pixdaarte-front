"use client"

import { useParams } from "next/navigation"

export default function Description(){

    // const params = useParams()
    // const slug = params.slug

    // const lastParams = slug?.[slug.length -1] //preparação para mandar identificador

    return(
        <div>
            <p className="text-black">Tela do pix/descrição do usuário</p>
        </div>
    )
}